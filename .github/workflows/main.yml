name: "Main Pipeline"
on:
  push:
  workflow_dispatch:
jobs:
  build:
    name: "🔨 Build"
    runs-on: ubuntu-latest
    steps:
      - name: "Repo Checkout"
        uses: actions/checkout@v4
      - name: "Setup Java environment"
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "oracle"
      - name: "Build with Maven"
        run: pushd backend; mvn compile package; popd

